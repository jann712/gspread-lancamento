from sheets_new import Sheets
# Create a list of integers and strings
my_list = []

# Use list comprehension to filter out the strings
print(len(my_list))

# Print the new list

# a = Sheets("MACRO", "Planilha5")
# b = [item[0] for item in a.get_sheet_values()]
# print(b)

